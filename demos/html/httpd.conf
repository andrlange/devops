ServerRoot "${SERVER_ROOT}"
Listen "${PORT}"
ServerName 0.0.0.0

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule mime_module modules/mod_mime.so
LoadModule dir_module modules/mod_dir.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule include_module modules/mod_include.so
LoadModule filter_module modules/mod_filter.so
LoadModule env_module modules/mod_env.so

ErrorLog /dev/stderr
CustomLog /dev/stdout combined
PidFile /tmp/httpd.pid
Mutex default:/tmp

TypesConfig conf/mime.types

# Pass CF environment variables to SSI
PassEnv VCAP_APPLICATION
PassEnv CF_INSTANCE_GUID
PassEnv CF_INSTANCE_INDEX
PassEnv CF_INSTANCE_INTERNAL_IP
PassEnv MEMORY_LIMIT
PassEnv DISK_QUOTA

DocumentRoot "/workspace/public"
DirectoryIndex index.html

<Directory "/workspace/public">
    Require all granted
    Options +Includes
    AddOutputFilter INCLUDES .html
</Directory>
