apiVersion: v1
kind: Service
metadata:
  name: artifact-keeper-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: artifact-keeper
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
