apiVersion: v1
kind: Service
metadata:
  name: trivy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trivy
    app.kubernetes.io/component: scanner
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP
