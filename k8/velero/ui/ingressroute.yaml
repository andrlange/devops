apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: velero-ui
  namespace: velero
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`backup.development.cfapps.cool`)
      kind: Rule
      services:
        - name: velero-ui
          port: 3000
  tls: {}
