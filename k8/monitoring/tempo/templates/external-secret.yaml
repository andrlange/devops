apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tempo-s3-credentials
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: tempo-s3-credentials
  data:
    - secretKey: access_key
      remoteRef:
        key: secret/garage/tempo
        property: access_key
    - secretKey: secret_key
      remoteRef:
        key: secret/garage/tempo
        property: secret_key
