.idea/
.env.local
k8/.env.local
k8/unseal.sh
/gcp-dns-credentials.json
k8/distribution/.install-config
k8/distribution/.install-state
.worktrees/
credentials.md
openbao_credentials.md
dist/

# macOS
.DS_Store

# Compiled Go broker binaries (build artifacts)
k8/services/cf-service-broker/src/cf-service-broker
k8/services/cf-marketplace-broker/src/cf-marketplace-broker

# Claude / local agent info — keep local only, never push to remote
CLAUDE.md
.claude/
.superpowers/

# Standalone remote-server stacks — kept in their own repos, not in this one
/artifactory/
/otel/
/router/
/vault/

# Temporary migration secrets (registry push token, etc.) — never commit
tmp.secret
*.secret
k8/distribution/.docker/
